- conference = nil
%h1 All events
- @events.each do |event|
	- unless event.conference == conference
		%h2= event.conference.name
		- conference = event.conference
		%div Events:
	%div
		%ul
			%li
				%a{:href=>"/conferences/events/#{event.id}"}= event.name
				= ": #{event.location}"
